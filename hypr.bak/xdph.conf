# xdg-desktop-portal-hyprland usa hyprlang (não migra para Lua)
screencopy {
    allow_token_by_default = true

    # TODO: instalar hyprland-preview-share-picker para um picker de compartilhamento com preview
    # custom_picker_binary = hyprland-preview-share-picker
}
